ea3.sh - apalyzer - apache access_log analyzer

Unzip the apalyzer_vX.zip (where X is the version) into the directory where you have the log files to analyze.

Then run just:
ea3.sh <access_log file name>

A zip file will be created in the same directory.
